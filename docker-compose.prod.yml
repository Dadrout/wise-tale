services:
  backend:
    build:
      context: ./wizetale-api
    container_name: wizetale-backend-prod
    env_file:
      - ./wizetale-api/.env
    restart: unless-stopped
    depends_on:
      - redis
    networks:
      - wizetale_net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  nginx:
    build:
      context: ./nginx # Use our custom Dockerfile for Nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
    depends_on:
      - backend
    networks:
      - wizetale_net

  redis:
    image: redis:alpine
    container_name: wizetale-redis-prod
    restart: unless-stopped
    volumes:
      - redis_data:/data
    networks:
      - wizetale_net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  redis_data:

networks:
  wizetale_net:
    driver: bridge 